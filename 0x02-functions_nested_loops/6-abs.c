#include "main.h"
#include <unistd.h>
#include <stdio.h>
/**
 * _abs - Something
 *
 * @i: ehkwbfehw
 *
 * Description: 'print_alphabet is a function'
 *
 * Return: Always 0.
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (i * -1);
}
