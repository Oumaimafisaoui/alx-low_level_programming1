#include "main.h"
#include <unistd.h>
/**
 * _strlen - takes a pointer and change its content
 * @s: the param
 * @b: ecfeffe
 * Description: rfrefr wedfweffew
 * Return: zero
 **/

int _strlen(char *s)
{
	int i;

	i = 0;
	while(s[i])
	{
		i++;
	}
	return (i);
}

