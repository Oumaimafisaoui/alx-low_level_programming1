#include "main.h"
#include <unistd.h>
/**
 * _strlen - takes a pointer and change its content
 * @s: the param
 * @b: ecfeffe
 * Description: rfrefr wedfweffew
 * Return: zero
 **/

int _strlen(char *s)
{
	while(*s)
	{
		write(1, &*s, 1);
		s++
	}
}

